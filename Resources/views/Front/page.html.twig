{% extends 'StemsPageBundle:Layout:' ~ page.layout.slug ~ '.html.twig' %}

{# default content page renderer uses layout defaults and page variables, this twig file is only necessary to begin climbing up through the relevant layout file #}

{# custom and dynamic pages can extend blocks here to add custom html and values in the controller #}